apiVersion: v1
kind: Service
metadata:
  name: {{ include "ecolibrium-app.fullname" . }}
  labels:
    {{- include "ecolibrium-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ecolibrium-app.selectorLabels" . | nindent 4 }}
